<%= form_with(model: banking) do |form| %>
  <% if banking.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(banking.errors.count, "error") %> prohibited this banking from being saved:</h2>

      <ul>
        <% banking.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

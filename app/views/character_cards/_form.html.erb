<%= form_with(model: character_card) do |form| %>
  <% if character_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(character_card.errors.count, "error") %> prohibited this character_card from being saved:</h2>

      <ul>
        <% character_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: dating) do |form| %>
  <% if dating.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dating.errors.count, "error") %> prohibited this dating from being saved:</h2>

      <ul>
        <% dating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

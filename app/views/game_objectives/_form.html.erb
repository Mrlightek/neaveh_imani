<%= form_with(model: game_objective) do |form| %>
  <% if game_objective.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game_objective.errors.count, "error") %> prohibited this game_objective from being saved:</h2>

      <ul>
        <% game_objective.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

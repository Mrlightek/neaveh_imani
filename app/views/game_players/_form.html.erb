<%= form_with(model: game_player) do |form| %>
  <% if game_player.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(game_player.errors.count, "error") %> prohibited this game_player from being saved:</h2>

      <ul>
        <% game_player.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

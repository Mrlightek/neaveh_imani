<%= form_with(model: human_resource) do |form| %>
  <% if human_resource.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(human_resource.errors.count, "error") %> prohibited this human_resource from being saved:</h2>

      <ul>
        <% human_resource.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

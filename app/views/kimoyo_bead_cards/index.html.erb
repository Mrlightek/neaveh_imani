<p style="color: green"><%= notice %></p>

<% content_for :title, "Kimoyo bead cards" %>

<h1>Kimoyo bead cards</h1>

<div id="kimoyo_bead_cards">
  <% @kimoyo_bead_cards.each do |kimoyo_bead_card| %>
    <%= render kimoyo_bead_card %>
    <p>
      <%= link_to "Show this kimoyo bead card", kimoyo_bead_card %>
    </p>
  <% end %>
</div>

<%= link_to "New kimoyo bead card", new_kimoyo_bead_card_path %>

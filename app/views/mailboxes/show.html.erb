<p style="color: green"><%= notice %></p>

<%= render @mailbox %>

<div>
  <%= link_to "Edit this mailbox", edit_mailbox_path(@mailbox) %> |
  <%= link_to "Back to mailboxes", mailboxes_path %>

  <%= button_to "Destroy this mailbox", @mailbox, method: :delete %>
</div>

<%= form_with(model: marketing) do |form| %>
  <% if marketing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(marketing.errors.count, "error") %> prohibited this marketing from being saved:</h2>

      <ul>
        <% marketing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

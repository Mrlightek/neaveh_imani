<%= form_with(model: money_pack) do |form| %>
  <% if money_pack.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(money_pack.errors.count, "error") %> prohibited this money_pack from being saved:</h2>

      <ul>
        <% money_pack.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

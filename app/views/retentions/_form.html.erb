<%= form_with(model: retention) do |form| %>
  <% if retention.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retention.errors.count, "error") %> prohibited this retention from being saved:</h2>

      <ul>
        <% retention.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

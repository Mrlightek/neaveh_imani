<%= form_with(model: streaming) do |form| %>
  <% if streaming.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(streaming.errors.count, "error") %> prohibited this streaming from being saved:</h2>

      <ul>
        <% streaming.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

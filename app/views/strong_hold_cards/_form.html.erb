<%= form_with(model: strong_hold_card) do |form| %>
  <% if strong_hold_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(strong_hold_card.errors.count, "error") %> prohibited this strong_hold_card from being saved:</h2>

      <ul>
        <% strong_hold_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

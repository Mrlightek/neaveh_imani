<%= form_with(model: transport_card) do |form| %>
  <% if transport_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(transport_card.errors.count, "error") %> prohibited this transport_card from being saved:</h2>

      <ul>
        <% transport_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

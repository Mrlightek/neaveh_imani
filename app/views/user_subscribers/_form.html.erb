<%= form_with(model: user_subscriber) do |form| %>
  <% if user_subscriber.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_subscriber.errors.count, "error") %> prohibited this user_subscriber from being saved:</h2>

      <ul>
        <% user_subscriber.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :to, style: "display: block" %>
    <%= form.number_field :to %>
  </div>

  <div>
    <%= form.label :from, style: "display: block" %>
    <%= form.number_field :from %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>

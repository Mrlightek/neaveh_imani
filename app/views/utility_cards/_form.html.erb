<%= form_with(model: utility_card) do |form| %>
  <% if utility_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(utility_card.errors.count, "error") %> prohibited this utility_card from being saved:</h2>

      <ul>
        <% utility_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

<%= form_with(model: villiage_card) do |form| %>
  <% if villiage_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(villiage_card.errors.count, "error") %> prohibited this villiage_card from being saved:</h2>

      <ul>
        <% villiage_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
